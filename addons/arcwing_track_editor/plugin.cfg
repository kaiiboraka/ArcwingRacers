[plugin]

name="Arcwing Track Editor"
description="Author track branches directly in the 3D viewport: color-coded gizmo on TrackSpline nodes for every path (main + alternates), draggable points and control handles with undo, and branch topology wiring."
author="Arcwing"
version="1.0.0"
script="plugin.gd"
